# Day routing — merged into `desk.md`

This spec and `desk-processing.md` were unified into **`docs/desk.md`**
(2026-08-07): one pile, one gesture, with the Day flow as the default
door. The decision log, Route model, and slices live on in that
document; the prototype record stays at `/prototype/day-routing`. Git
history holds the full original.
