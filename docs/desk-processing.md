# Desk processing — merged into `desk.md`

This spec and `day-routing.md` were unified into **`docs/desk.md`**
(2026-08-07): one pile, one gesture. The rail, Lenses, and slice history
described here live on in that document; the `r`/`n`/`x` filing gestures
it specified are superseded by routing (ADR 0017). Git history holds the
full original.
