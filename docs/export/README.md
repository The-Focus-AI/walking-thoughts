# Account export (#16)

Portable full-account export: every active Thread with its chronological
Captures and Enrichments in readable Markdown, complete JSON records with
stable identifiers and source metadata, and original synchronized media with
unambiguous references from both representations.

Work in progress. The export format and access seams will be documented here.
