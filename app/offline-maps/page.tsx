import { OfflineMapsPage } from "@/components/offline-maps-page";

export const metadata = {
  title: "Offline — Walking Thoughts",
  description: "Download the Offline Region trail map pack for airplane mode.",
};

export default function OfflineMapsRoute() {
  return <OfflineMapsPage />;
}
