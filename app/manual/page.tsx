import { ManualSheet } from "@/components/manual-sheet";

export const metadata = {
  title: "Field Manual — Walking Thoughts",
  description:
    "What happens to a thought between the trail and the place it ends up.",
};

export default function ManualPage() {
  return <ManualSheet />;
}
