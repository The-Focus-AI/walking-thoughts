/** Instant feedback for a You tab tap while the route loads on a slow link. */
export default function InterviewLoading() {
  return (
    <p className="route-loading" role="status">
      Opening You…
    </p>
  );
}
